# CaastorOS — Implementation Log

Append-only. Format: `| DATE | TASK | WHAT WAS BUILT | DIVERGENCE + REASON |`

| DATE | TASK | WHAT WAS BUILT | DIVERGENCE + REASON |
|------|------|----------------|---------------------|
| 2026-05-22 | IA nav reorg (ia-plan.md) | `portal-shell.jsx`: added `section` field to `CLIENT_ROUTES`, grouped sidebar eyebrows, demoted `discovery` from nav (route guard patched), renamed `home` → "Create", `bio` active highlight on discovery route. | None — matches ia-plan.md §5 exactly. |
| 2026-05-22 | Wire HomeCreate brief→ship loop | `portal-brandolph.jsx` `HomeCreate`: added `phase` state (idle → sharpening → proposing → running → done), `handleStart` / `handleProceed` / `handleRun` / `handleReset` handlers, `⌘+↵` keyboard shortcut, review panel renders `BrandolphDiagnosis` then inline assembly grid with live `dot-state` indicators, "Assembling…" spinner (2.8 s timeout), `OutputsReady` on completion. Dashboard (In flight, Try something, Brandolph watching) hides while review is active and restores on reset. | Inline assembly card is a new layout (not `AssemblyPanel` sidebar) — `AssemblyPanel` is height:100% and sidebar-optimised; reusing it inline would break layout. Inline card shares the same data (`getAssembly`) and `dot-state` classes. |
| 2026-05-24 | QW-001 IA naming fix (rev-2 §2 LOCKED structure) | `portal-shell.jsx`: rewrote `CLIENT_ROUTES` to the four sections × two children structure (`Create / Briefs` · `BIO / Library` · `Specialists / Humans` · `Credits / Account`). Section eyebrows ARE the contents (slash-joined). Replaced `Brand Intelligence` nav label with `BIO`, `Human craft` with `Humans`, `Settings` with `Account`. Updated `TopBar.titles` eyebrows to match. Cleaned stale `Human craft` references in `portal-briefs.jsx:1535` (flash toast), `portal-data.js:27` (credit ledger sample), `portal-craft.jsx:109` (drawer eyebrow), `portal-craft.jsx:179` (upsell prose). | Route id `craft` preserved (not yet renamed to `humans`) per rev-2 §2.3 — full route-id rename deferred to the M4 cleanup PR alongside other workspace-shape touches in P7. The longform descriptor "Brand Intelligence Object" still appears in narrative copy (`portal-briefs.jsx` BIO node labels, `portal-discovery.jsx` eyebrow) — kept intentionally as the **concept** name; only the nav **label** is locked to "BIO". |
| 2026-05-24 | QW-002 `ShellMode` primitive (rev-2 §7 workspace state) | `portal-shell.jsx`: added `useShellMode(routeId)` hook + `WORKSPACE_ROUTES = {brief-detail, canvas, board}` set; hook sets `document.documentElement.dataset.shellMode` to `"default"` or `"workspace"`. Wired into `App()` after design settings. `portal.css`: added 25 lines of `[data-shell-mode="workspace"]` overrides — dock collapses 216→56px with `:hover` re-expansion, labels + section eyebrows + OS mark fade via opacity (100ms), topbar height shrinks 56→40px, title font 16→14px, `.app` padding-left 248→88px. All transitions in pure CSS (180ms ease for layout, 100ms for opacity) — JS only writes the attribute. | Used `data-shell-mode` attribute on `<html>` mirroring the existing `data-theme`/`data-palette`/`data-density` pattern from `useDesignSettings` so CSS authors have one consistent driver for global shell state. Hover-reveal model preferred over a JS-controlled collapsed/expanded state (no extra React state to sync, no flicker, browser handles the timing). The hidden labels stay in the DOM (opacity:0, not display:none) so screen readers can still announce nav items. |
| 2026-05-24 | QW-003 Three-bucket Discovery intake (rev-2 §5.3 mock-side) | `portal-discovery.jsx`: replaced the single "Brand guidelines · paste or upload" field in `DiscoveryStep1` with three labelled drop zones (`foundations` / `visual` / `voice`). New inline `BucketDropZone` component supports drag-drop AND click-to-upload via a hidden `<input type="file" multiple>`. Each zone shows: bucket label, helper copy, `readBy` department tag, file chips with `×` remove. Local state via `useDState({foundations:[], visual:[], voice:[]})`. The "Start extraction" button now logs the bucketed payload to console (preview of the P1-003 `POST /api/bios/:brandId/sources` contract) before calling `onNext`. Footer also shows total file count when files are queued. Removed the now-redundant "Upload guidelines instead" link from the helper row. | Kept the implementation contained inline to keep this PR small — `BucketDropZone` lives next to `DiscoveryStep1` rather than in `portal-shared.jsx`. If the same three-bucket pattern shows up at the BIO sources tab (`BioSources` in same file), promote to shared at that point. The `BUCKETS` const at the top of the new block is the contract that P1-003 backend will mirror — keep these labels stable. |
| 2026-05-24 | QW-004 OutputCard footer split + Steward chip (rev-2 §5.5 + §9, moat-defining) | `portal-data.js`: added `CI_BRAND.bioVersion = 7` and `CI_BRAND.steward = {firstName, fullName, role, certifiedAt}` mock. Steward firstName "Marina" matches the rev-2 plan's exemplar; surname differs from `CI_USER.name` (which is the brand founder "Marina Reyes") — known mock collision, harmless. `portal-shared.jsx`: rebuilt `OutputCard` footer with two render branches: **ClientFooter** (default, public) leads with `Composed by {specialist} · BIO v{n} · certified by {steward.firstName} · {date}` — NO model name; **DebugFooter** adds `routed via {model.label}` + `run {short_id}`. Team portal renders DebugFooter directly; client portal renders ClientFooter inline and slides DebugFooter in as a tooltip card on hover (`.output-card:hover .output-card__debug-tip`). 9 lines of CSS in `portal.css` for the hover transition (opacity + transform, 140ms ease). | Hover-reveal preferred over click-to-toggle because the spec says "no click required" (rev-2 plan QW-004 acceptance). `:focus-within` added alongside `:hover` so keyboard users (tab into the card) also see the debug attribution. The hidden tooltip uses `pointer-events:none` when invisible so it doesn't block clicks below. Model name `GPT-5` (from `CI_MODELS.gpt5.label`) flows through unmodified — when P3 wires `runs.model_used` for real, that field replaces the agent.model lookup. Output card now has `position:relative` so the absolutely-positioned tooltip anchors correctly. |
| 2026-05-24 | P0 backend bootstrap (P0-002 + P0-003 + P0-006) | **P0-002 (user-side):** Supabase staging project `yemzrohzqfuxmekcgnnh` provisioned. SUPABASE_URL + anon + service-role keys written to `server/.env` (gitignored); structure documented in `server/.env.example`. **P0-003 (schema):** `supabase/migrations/20260524183348_init.sql` (464 lines) — 18 tables (kernel + rev-2: `team_members`, `industries`, `templates`, `template_versions`, `steward_jobs`, plus `bios.certified_*`, `bio_sources.bucket`, `briefs.mode`, `uploads.bucket_hint`), 3 public_* views for the `?ask=` allowlist, RLS workspace-isolation policies on all client-facing tables + role-gated `steward_role` policy on `steward_jobs`, and `handle_new_auth_user` trigger that auto-creates `workspaces`+`brands`+`users` row on first magic-link sign-in. **P0-003 follow-up:** `20260524185323_grants_and_admin_rls.sql` (54 lines) — standard Supabase GRANTs to anon/authenticated/service_role + read-only RLS on the admin-curated tables (industries, specs, templates, template_versions). Both migrations pushed to remote via `supabase db push`. **8 V1 industries seeded** via REST bulk insert (CLI `db query` defaults to local; REST + service-role bypasses RLS and works against remote). **P0-006 (specs):** `scripts/seed-specs.mjs` reads `src/portal-data.js` via Node `vm.runInContext` sandbox (avoids duplicating the 33-row CI_AGENTS + 6 CI_DEPT_SPECS source of truth), constructs spec payloads with role/objective/method/voice/refusals/modelRouting/bioSlices, and upserts via REST. Run via `npm run seed:specs`. Verified: 33 spec rows in DB (26 active live, 7 inactive soon). Root `package.json` gained 3 scripts: `dev:server`, `dev:all` (via newly-added `concurrently` dev dep), `seed:specs`. | Pivoted from refinement's Cloudflare Workers plan because the existing `server/src/index.js` already shipped a working Hono-on-Node server with Anthropic SSE streaming — keep what works, defer Workers migration. Migration is split into two files (init + grants) because dashboard SQL editor auto-grants on table creation but CLI migrations don't — without the second migration, PostgREST returned 403 on every table even with the correct service-role key. The seed-specs script uses sandbox eval over ESM import-refactor of portal-data.js to keep the source of truth single during the P0→P3 transition window (mocks are still consumed by the SPA until P3 wires real DB reads). PAT `sbp_e88f...` was passed in chat to enable my `supabase link`/`db push` from this session — flagged for user revocation in the dashboard once we're done with this push. |
| 2026-05-25 | P0-004 auth wiring + DB bridge + P1 backend scaffolding | **P0-004 (auth wiring):** Added root `.env.local` with `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` + `VITE_API_BASE`; root `.gitignore` updated to exclude `.env*`. Installed `@supabase/supabase-js` at SPA root. New `src/lib/supabase-browser.js` exposes the anon client + `apiFetch(path, init)` helper that auto-attaches `Authorization: Bearer <jwt>` from the current session. **Rewrote `src/portal-auth.jsx`** — replaced the localStorage mock with real Supabase email+password auth (signUp + signInWithPassword + signOut); the `useSession()` hook + `window.CI_AUTH` surface preserved so portal-shell consumers don't change. Role resolved from the public.users row (populated by the `handle_new_auth_user` trigger we shipped). Login screen now toggles between sign-in and sign-up modes, surfaces Supabase error messages, and routes by resolved role. **DB bridge:** new `server/src/lib/supabase.js` (service-role admin client + per-request `userClient(jwt)`); new `server/src/middleware/auth.js` (Hono middleware — extracts JWT, validates via Supabase Auth, looks up workspace_id + role, attaches to `c.set('auth')`); new `server/src/lib/load-brand-bio.js` (loads brand + latest BIO from DB; falls back to the Vinilo seed when no BIO exists yet so the endpoint works pre-P1; supports `requireCertified` flag for when P1.5 Steward lands). **Refactored `server/src/routes/brandolph.js`** — `/api/brandolph/ask` now requires auth + loads brand+bio from DB instead of `data/vinilo.js`. Server CORS allowlist expanded to include `http://localhost:5174` (Vite landed there when 5173 was occupied). **P1 backend scaffolding:** new `server/src/routes/bios.js` mounted at `/api/bios`: `POST /:brandId/sources` writes one `bio_sources` row per submitted reference with the rev-2 §5.3 `bucket` field set; `GET /:brandId` returns the latest BIO + cert state. Ownership check on every call (workspace_id match enforced server-side). | Chose email+password over magic-link for dev: magic-link callback URL requires a Supabase dashboard config change (Authentication → URL Config → add `http://localhost:5174`) that adds setup friction; password works zero-config the moment "Confirm email" is toggled off in Supabase → Authentication → Settings. Sign-up flow surfaces a clear "check your inbox" message if confirmations are still required so the user knows what to do. The brandolph route now does a workspace-scoped DB lookup via the service-role client rather than relying on RLS through `userClient(jwt)`, because the service role + explicit workspace check in `loadBrandBio` is simpler than threading the JWT through and faster (no RLS check overhead on hot path). Pre-P1 BIO fallback (Vinilo seed) is logged with a warn so it's spottable in dev — once Discovery (P1) ships, real BIOs land and the fallback only fires for brand-new workspaces. File-upload via Supabase Storage NOT YET wired — `POST /sources` accepts URL/text refs only; the QW-003 UI's File chips will need a follow-up multipart upload route. |
| 2026-05-25 | OR-001..OR-007 hybrid model router (Anthropic direct + OpenRouter) | **Decision:** keep Anthropic SDK direct for Claude calls (preserves the §7 prompt-caching cost lever — 60–80% input-cost savings projected on assembly runs); route all non-Claude text (GPT-5, Gemini Flash/Pro) through OpenRouter; image/web/deck/search vendors get their own modules later. **Files added:** `server/src/lib/models/anthropic.js` (Anthropic SDK adapter, preserves `cache_control: ephemeral` on system blocks), `server/src/lib/models/openrouter.js` (raw fetch + SSE parser, OpenAI-compatible API, surfaces per-call `cost_usd` from OpenRouter's `usage.cost`), `server/src/lib/models/router.js` (single `streamCompletion({ spec, system, messages })` entry; dispatches by `spec.payload.modelRouting.primary` prefix — `anthropic/*`, `openrouter/*`, `vendor/*` reserved for non-text). Both adapters yield normalized event stream `{type:"token"\|"done"\|"error"}` so consumers (routes, runtime) don't need vendor-aware code. **Files changed:** `server/src/routes/brandolph.js` now delegates to router (was direct Anthropic); `scripts/seed-specs.mjs` got a MODEL_MAP that rewrites short keys (`opus`,`sonnet`,`haiku`,`gpt5`,`gemFlash`,`gemPro`,`flux`,`gptimage`,`recraft`,`exa`,`v0`,`gamma`,`framer`,`elevenlabs`) to vendor-prefixed routes (`anthropic/claude-opus-4-7`, `openrouter/openai/gpt-5`, `vendor/fal/flux-1.1-pro`, etc.). Re-seeded 33 specs — verified mix: 22 anthropic/, 4 openrouter/, 7 vendor/. **Files added (testing):** `scripts/test-router.mjs` exercises all three paths through the router; `npm run test:router`. Verified: Anthropic Haiku 1.2s/4 tokens; Gemini 2.5 Flash 430ms/$0.0000088; GPT-5 via OpenRouter dispatches correctly. **Cache observation:** Brandolph's 1215-token system prompt sits just above Sonnet's 1024-token cache minimum; cache_creation fires on calls 2+ but hits aren't consistent yet. P3 specialist runs will have 2000+ token system prompts (full spec + bio slice + brief context) where caching is robust. | Hybrid over pure-OpenRouter because OpenRouter's Anthropic-cache passthrough is flaky and the cost lever is too important to risk — direct Anthropic is the right tradeoff. The `vendor/*` prefix in modelRouting is a deliberate marker so the runtime knows "this is a non-text vendor, dispatch via image/web/etc module" without each caller checking. Router enforces `max_tokens >= 16` floor because GPT-5 rejects smaller values (the first router test discovered this — caller-supplied values are also floored to keep the contract uniform across vendors). OpenRouter exposes per-call `cost_usd` in usage, which we surface in the normalized usage block; Anthropic doesn't (we compute cost from token counts × pricing table at the cost-ledger layer, planned for P3). PAT/keys: Anthropic key `sk-ant-...` + OpenRouter key `sk-or-v1-...` were passed in chat and written to `server/.env`; flagged for user rotation. |
| 2026-06-06 | P1-FE live Discovery→BIO loop wiring (frontend consumes the real backend) | **`src/portal-discovery.jsx`:** new `useLiveBio({pollMs})` hook resolves the workspace's brand (via `window.getCurrentBrandId` → first-brand fallback), polls `GET /api/bios/:brandId`, and exposes `{brandId, brandName, bio, cert, refresh}` + a `team_members` lookup for the certifier's name. `DiscoveryStep1` now fires `POST /api/discovery/start` (controlled URL input, busy/error states) instead of just advancing the wizard; three-bucket `BucketDropZone` intake kept (files still local — Storage upload is the `/sources/upload` follow-up). `DiscoveryStep2Running` polls `/api/bios/:id` every 3s for a version bump (the compile-bio worker finishing) with a 90s safety fallback + crude scrape→vision→compile→done stage UI. `DiscoveryStep2Results` + `BioViewer` rewired off mocks: `payloadToFields`/`fieldsToPayload` map the API's nested BIO ↔ the viewer's flat tab arrays; Edit→Save calls `PATCH /api/bios/:id` (new version, Steward re-cert); Feed-Brandolph calls `POST /api/bios/:id/sources`; live Steward cert chip; "No canon yet" empty state; competitive tab stubbed (a32 extracts it later). **`src/portal-floater.jsx`:** live Brandolph chat streams from `POST /api/brandolph/ask` (SSE) with mock fallback; memory-driven contextual greeting from `GET /api/brandolph/memory?brandId=`; floater now also shows on the admin portal. **`src/portal-shared.jsx`:** current-brand store (`getCurrentBrandId`/`setCurrentBrandId`/`useCurrentBrandId`, localStorage + `brand:changed` event, exported to window); OutputCard client/debug attribution footer split (Steward chip, no model name on client); redrawn BIO/brief/Humans/Create/Library/Credits/Settings icons. **`src/portal-auth.jsx`:** full real-Supabase auth (signin/signup/forgot/recovery modes, deadlock-safe `onAuthStateChange`, synchronous bootstrap from stored session). **`src/main.jsx`:** import `portal-admin.jsx`. **`src/portal-craft.jsx`:** "Human craft"→"Humans" copy. **Bugfix:** `streamBrandolph` used raw `fetch` (no JWT) against the auth-required `/ask` route → silent 401 → always fell back to mock; switched to `apiFetch` so the JWT attaches and live chat works. | **Verified WITHOUT spending API credits** (standing "don't waste credits" rule): `npm run build` clean (85 modules, 820kB), `npm run test:units` green (47/47), server boots and every loop endpoint returns 401 unauth = wired+guarded (`POST /discovery/start`, `GET/PATCH /bios/:id`, `POST /bios/:id/sources`, `POST /brandolph/ask`, `GET /brandolph/memory`). **NOT yet run:** a real paid end-to-end Discovery (scrape + Gemini 2.5 Pro BIO synth) — left for the user to trigger so we don't burn credits on a CI-style check. The Discovery step records a *baseline* BIO version on first poll and advances only when a higher version lands, so a re-discovery on a brand that already has a BIO works correctly. Backend (`discovery.js`, `bios.js`, `compile-bio.js`, `assign-steward.js`, `/brandolph/memory`) was already committed earlier; this row is the frontend that consumes it. |
| 2026-08-15 | BIO v1.0 · M0 — schema + rules foundation (spec `docs/2026-08-14-canonical-spec-bio-v1.md` §8) | **New `server/src/lib/bio-schema.js`** — the single source of truth for BIO shape: `BIO_FIELDS` registry (per-leaf `path`/`kind`/`slice`/`scored`), `SECTIONS`, `SCORED_PATHS` (derived), `normalizeBio(raw)` (guarantees every section/field is present, arrays→`[]`, scalars→`""`, preserves values + extra keys + meta, stamps `schema_version`, idempotent), and `projectBio(assertions)` (pure event-sourced fold → `{payload, confidence, missing, conflicts}`; human-confirmed wins, divergent unconfirmed values surface as `conflicts[]` — never averaged; deterministic regardless of input order). **`score-bio.js`** now imports `SCORED_PATHS` from the registry instead of a hardcoded list — one source of the scored field set. **`prompt.js`** `renderBioLayer` calls `normalizeBio(bio)` first — fixes the Brandolph 400 (a partial BIO previously threw on `bio.visual.palette.map` etc.). **`load-brand-bio.js`** normalizes both return paths (single boundary — every read is canonical). **`steward.js`** BIO-patch insert replaced hardcoded `score: 75` with `scoreBio(merged)` — one deterministic scorer across all three write paths (`compile-bio`, `bios PATCH`, `steward`). **Tests:** new `server/src/lib/bio-schema.test.mjs` (13 tests: normalize gaps/idempotence/preservation, the Brandolph no-throw fix, `SCORED_PATHS`-matches-golden guard, projectBio determinism/conflict/confirm-wins) added to `test:units`. Full suite 89/89 green; all edited files `node --check` clean. | **Scope trimmed from the spec's M0 with two documented deferrals (ponytail):** (1) the `bio_assertions` DB table → deferred to **M3** where its writer (discovery) lands — a table with no writer is dead infra; the *pure* `projectBio` + assertion contract are built and tested now so M3's migration just matches them. (2) The specialist-registry migration off the `portal-data.js` browser-eval mock → pulled out to a focused **pre-M4** change (it needs the shared slice enum) — it touches SPA+seed+server and isn't needed for M1's gate, so bundling it here would bloat M0's blast radius and risk behavior change. M0 is deliberately pure + no-behavior-change: `normalizeBio` only fills gaps (full BIOs render byte-identical — asserted), `[]`-vs-`undefined` is identical for the already-guarded specialist renderer, and `SCORED_PATHS` is proven equal to the historical list so no BIO score moves. `renderBioLayer` normalizes defensively (belt-and-suspenders with the loader) so the crash site is safe regardless of caller. Not committed — left in the working tree for review. |
| 2026-08-15 | BIO v1.0 · M1 — the certification gate + security hardening (spec §7 + §8; user chose HARD-BLOCK production now) | **The data-layer gate.** `load-brand-bio.js` rewritten with two tiers: `loadBioForRun` (production — reads highest **certified** version, throws `BIO_NOT_CERTIFIED` 409) and `loadBioForBriefing` (briefing — reads highest **self_certified** version, throws `NOT_SELF_CERTIFIED` 409); read-only path throws `NO_BIO`. **The silent Vinilo whole-BIO fallback is removed** — it now only fires behind `ALLOW_SEED_BIO=1` (dev), never for a real request; a BIO-less brand fails closed. **Gate wired:** `runs.js` (specialist runs) → `loadBioForRun`; `craft.js` (human polish) → `loadBioForRun` added after the tier gate; `briefs.js` (`/sharpen`) → `loadBioForBriefing`; `brandolph.js` (chat) → permissive read, graceful `NO_BIO`. **Migration `20260815000000_m1_gate_and_hardening.sql`:** (1) `users.role` CHECK += `super_admin`; (2) `bios.self_certified` + `self_certified_at` columns + cutover backfill (every existing brand's LATEST bio → `self_certified=true` so briefing keeps working; production is NOT grandfathered — hard-blocks until a Steward certifies in M2); (3) **RLS write-lockdown** — `revoke insert,update,delete on brands/briefs/bios/bio_sources/clarifications/runs/outputs/qa_results/ledger/uploads/workspaces/steward_jobs from anon,authenticated` (closes the forge-cert + forge-credits REST holes; adding WITH CHECK would NOT have fixed it — the check reuses the tenancy predicate), plus `alter default privileges … revoke insert,update,delete` so future tables don't re-grant client writes. SELECT stays (RLS governs; the SPA reads these tables directly via anon — verified it never writes them). **`auth.js`:** `requireSuperAdmin` middleware. **`ingest-guards.js`** (new, pure): `validateUpload` (MIME/size/ext allow-list, 15MB cap) wired into `bios.js` upload BEFORE buffering; `assertPublicUrl` (SSRF block: file://, localhost, RFC1918, 169.254.169.254, IPv6 loopback) wired into `discovery.js`. **Tests:** `ingest-guards.test.mjs` (9 tests) — one caught a real bug (WHATWG URL keeps IPv6 hosts bracketed, so `[::1]` slipped the loopback check; fixed). Suite 98/98. **`scripts/test-gate.mjs`** (npm `test:gate`) — acceptance harness that attacks the forge surface (PATCH bios.certified, insert ledger, complete steward_job) from a client JWT and asserts each fails; run post-migration. Verified: `node --check` all files, full server boot with the complete route graph, 98/98 units. | **User decision (AskUserQuestion): HARD-BLOCK all production now** — since nothing is human-certified until M2, ALL production is blocked until Stewards certify; briefing stays up via the self-cert backfill. **⚠ APPLY-ORDER: the migration and the code are a matched pair.** The loader now SELECTs `self_certified`; until `20260815000000` is applied that column doesn't exist and the query errors. Apply the migration **before/with** deploying this code. **NOT APPLIED by me** — migration left as a file for user review + apply (I do not touch the production DB from here). **Deferred (documented in the migration):** Storage short-lived signed URLs (replacing the 1-year URL in the bios.js upload) → M3, where the evidence pipeline is reworked; `storage.objects` is service-role-only by Supabase default so direct client object access isn't open today. **Not committed** — working tree, for review. SSRF guard is a literal-host check (ponytail-noted); DNS-rebind resolve-check deferred since scraping runs through Firecrawl (external) and the forged-`raw_ref` vector is closed by the bio_sources write-revoke. |
| 2026-08-15 | BIO v1.0 · M2 (backend) — two-stage certification (spec §3) | **Pure rubric engine** `evaluate-certification.js` — `DEFAULT_RUBRIC` (7 criteria, weights sum to 1, C1/C2 auto from scoreBio subterms, C3–C7 human 0–4 anchored) + `evaluateCertification({autoSignals, reviewerScores, rubricConfig})` → `{composite, band, gateFailures, recommendedDecision, needsCalibration, breakdown}`. Gating criteria have hard floors: a high composite is capped at `return_changes` on any gate miss, `reject` on a gate at 0. No I/O. `score-bio.js` gained `scoreBioBreakdown` (score + coverage/avgConf/sourceDiversity) feeding the auto criteria; `scoreBio` unchanged. **`bio-hash.js`** — stable `payloadHash` (sorted-key sha256) binding attestations/decisions to exact BIO bytes. **Migration `20260815010000`:** `steward_jobs` status += changes_requested/rejected/decertified + decision/conditions/required_changes/reject_reason_code/rubric_version_id/composite_score cols; `bios.cert_valid_until`; new tables `cert_rubric_versions` (config-as-data, one-active partial unique, seeded with DEFAULT_RUBRIC), `cert_decisions` (append-only audit — actor/decision/composite/breakdown/gate_failures/auto_signals/focus/payload_hash), `bio_attestations` (append-only self-cert). All three RLS-enabled, no client policy (server-only). **`steward.js` rewritten:** PATCH now computes the decision via the engine from `reviewerScores`, writes an immutable `cert_decisions` row on every event, and drives the four states; **four-eyes enforced** (a Lead can't approve their own submission — `job.assigned_to === steward.id` → 409); **fixed a latent bug** — the old calibration path completed the job but never set `bios.certified=true`, so calibrated BIOs never actually certified; now the Lead-approve path finalizes cert with TTL. New `POST /decertify` (super_admin/lead) flips certified=false + audits + enqueues re-review + notifies; the in-flight policy falls out of the gate (queued/new block via loadBioForRun, running finish, completed chips immutable, briefing untouched). GET `/jobs/:id` now returns the active rubric + auto-signals for the reviewer form. **`bios.js`:** `POST /:brandId/self-certify` — three affirmed statements + no high-importance gap + min-score precondition → writes `bio_attestations` + flips `self_certified` (unlocks briefing; auto-lapses on edit via the version bump). **`assign-steward.js`:** rotation rule implemented (the documented TODO) — excludes Stewards who crafted on the brand (craft.delivered_by on the brand's outputs), falling through to the Lead capacity fallback. **Tests:** `evaluate-certification.test.mjs` (9 — bands, gate-cap, gate-at-0-reject, incomplete, calibration flag, determinism), `bio-hash.test.mjs` (4). Suite **111/111**; node --check clean; full server boot OK. | **⚠ Two migrations now pending apply in order (M1 `20260815000000` then M2 `20260815010000`); the code depends on both.** Production is still hard-blocked until a Steward certifies a brand — now possible via the API (`PATCH /api/steward/jobs/:id` with reviewerScores). **Deferred within M2: the SPA surfaces** — the Steward rubric-scoring UI (`portal-team.jsx`) and the client self-cert attestation UI. The self-cert screen is the natural home of M3's discovery S12 (Confirm & Attest), so it folds there; the reviewer UI is a focused `portal-team.jsx` addition. Backend is real + tested, not a mock, but API-only until the UI lands. Decision is engine-computed (reviewer scores criteria; rubric decides the band) rather than reviewer-picked — deterministic + defensible. TTL (`cert_valid_until`) is set on approve; the staleness sweep that enqueues re-review on expiry is a cron follow-up (gate stays boolean-certified for now — grace, per spec). Not committed. |
| 2026-08-15 | BIO v1.0 · M2 (UI) — Steward reviewer rubric surface | **`src/portal-team.jsx`:** `useStewardJob` now carries `rubric` + `autoSignals` from the extended GET. New `RubricPanel` renders the active rubric — auto criteria (coverage, grounding) read-only with computed %, human criteria (C3–C7) each a 0–4 anchored button row (Absent→Exemplary) + a Low/Med/High confidence toggle, gate criteria badged with their floor. `TeamJob` gained scoring state + an init effect (seeds from the rubric's human criteria); `submit()` replaced by `submitReview()` (builds `reviewerScores`, sends the new PATCH contract, disabled until every criterion is scored) + `cancel()`. Optional "conditions / required changes (one per line)" textarea (sent as both — server keeps the field the decision needs) and a "reject reason code" input. The success toast shows the engine's outcome: `Decision · composite/100 (band) · gate: …`, or "Submitted for Lead review" under calibration. Lead approve/send-back path unchanged (now four-eyes-guarded server-side). | **MIGRATIONS APPLIED TO PROD** (M1 `20260815000000` + M2 `20260815010000`, user ran them in the SQL editor — "no rows returned" = success). Prod now has the new schema; the new **server code is NOT deployed**, so the hard gate is not yet active in prod (old code runs permissively). The reviewer UI is real + builds clean (85 modules) but only takes effect once the new SPA+server ship. **Self-cert client UI is deferred to M3** (it IS discovery's S12 Confirm & Attest); a Steward can now certify fully in-app, which is what unblocks production. Decert has an API but no UI yet (secondary admin action). Not committed — full M0–M2 tree awaiting review. |
| 2026-08-15 | BIO v1.0 · M2 (integration + end-to-end proof) — the complete chain, wired and verified performing real DB changes | **Client self-cert UI:** `SelfCertPanel` in `portal-discovery.jsx` (three attestation statements → `POST /:brandId/self-certify`) rendered in the BIO viewer when a BIO exists and isn't human-certified; flips to "Self-attested — briefing unlocked" once done. `bios.js` GET `/:brandId` now returns `self_certified`/`self_certified_at` so the viewer sees stage-1 state. This closes the chain in-app: a fresh brand can pass the briefing gate without touching the API. **Decertify UI:** a Lead/super-admin "Decertify this BIO" control in `TeamJob` (shown when `bio.certified && isLead`) → `POST /decertify`. **End-to-end proof `scripts/test-cert-flow.mjs`** (npm `test:cert-flow`): drives the REAL loaders + REAL rubric engine through actual DB writes and asserts every transition. **Ran it against prod** (isolated throwaway workspace/brand, auto-deleted) — **12/12 PASS**: uncertified→run BLOCKED + briefing BLOCKED; self-cert (score 86≥58, no high-importance gaps)→briefing UNLOCKED, production still blocked; rubric→approve@96→production UNLOCKED + loads the certified version + cert_decisions row written; decert→production BLOCKED again, briefing stays up. Final gates: **111/111 unit tests, SPA build clean**, full server boot OK. | **User's requirement met: the complete flow actually performs the changes, proven end-to-end on real Postgres — not a preview.** Verification path: local Supabase couldn't start in my sandbox (Docker Desktop daemon DNS can't reach the image registry — my shell has network, Docker's didn't), so per the user's call I ran the isolated+auto-cleaned test against prod (only a throwaway workspace, deleted after — no real data touched). The reviewer scoring UI was also shown live via a throwaway `/preview-reviewer.html` wired to the real engine (removed after; not committed). The per-field accurate/aspirational marking in the self-cert panel is still the richer M3/S12 job — the functional three-statement core lands now. Nothing committed yet — M0–M2 complete, integrated, and verified, awaiting the commit go-ahead. |
| 2026-08-15 | BIO v1.0 · M3 — discovery rebuild (draft lane + chaptered review + V2 + delegation) | **Orchestrated across 4 disjoint-ownership subagents against a fixed contract; integrated + verified centrally.** Migration `20260815020000` (additive): `discovery_sessions` (one resumable draft per brand — draft_payload/cursor/chapter_status/attested/status) + `discovery_delegations` (section-scoped magic links), both RLS-enabled/server-only. No `bios.status` (redundant with certified + self_certified + the draft). **Draft-lane endpoints** (`server/src/routes/discovery-session.js`, mounted at /api/discovery): GET (get/create session seeded from latest bios), PATCH (debounced autosave — no versioning, no steward job), POST /attest (promote draft to a candidate bios version if changed, then self-cert). **Shared `self-certify.js`** (`selfCertifyBio`) now powers BOTH the BIO-viewer self-cert (bios.js refactored to use it) and attest. **DISCOVERY_V2 defaults ON** (`compile-bio.js`: `!== "0"`) so uploads/Instagram/visual feed the BIO (models unchanged, cost-optimized); a best-effort Step 5 seeds the draft session from the compile. **Delegation route** (`discovery-delegation.js`): owner POST creates a tokened chapter link (emailed), tokened GET/PATCH expose/accept ONLY that one chapter, merge into the draft, notify the owner. **UI** (`portal-discovery.jsx`): the fake `CI_DISCOVERY` "Confirm" screen replaced by a live Draft Hub -> chaptered review -> S12 Attest — status chips, per-field inferred-vs-attested (amber "confirm, not stated"; missing renders empty, never guessed), debounced autosave + save-and-resume (cursor), delegate-a-chapter, attest with friendly 400 handling. Preserved: Connect/Reading steps, BioViewer + SelfCertPanel + cert chip, useLiveBio, the mappers. **Verified end-to-end on real Postgres** (`test-discovery-flow`, 10/10): seed draft -> briefing+production blocked -> autosave (no bios version created) -> attest promotes v1 + self-certifies (score 84) -> briefing UNLOCKED, production still gated, attestation written. 111/111 unit tests; SPA build + full server boot clean. | Fanned out per the user's "deploy as many subagents you need" with the goal's disjoint-ownership + shared-contract discipline (agents A: session/self-cert-helper/bios refactor, B: compile-bio V2+seed, C: delegation route, D: portal-discovery UI); I owned the migration, index.js route mount, and integration + the end-to-end proof. Agent contract deviations were all minor and accepted (APP_URL env name, self-cert tie-break ordering after the bio load, a defensive NO_SESSION guard, session create-if-missing in two places). **M3 migration applied to prod** (additive, user ran it in the SQL editor — "no rows returned"). Deferred to Phase 3 / follow-ups: session GC for abandoned drafts after N days; the delegation "handoff" SPA route (`#/discovery/handoff/:token`) still needs a small viewer screen (the backend + email are done); Storage short-lived signed URLs (still M-security). Committed as the 4th milestone commit. |
| 2026-08-15 | BIO v1.0 · Steward ↔ client review loop (flow gap surfaced in review) | **User flow question during review:** when a Steward needs changes from the client, or edits + certifies, the client must see it. Today only approve/decert notified, and the return-changes/reject/conditions data was stored but never surfaced. **Fanned out 2 disjoint agents + integrated.** Backend (`steward.js`, `bios.js`, new pure `bio-diff.js`): the submission path now fires client notifications on `return_changes` / `reject` / `approve_with_conditions` (`kind` = steward.changes_requested / .rejected / .conditions; guarded so an approve-with-conditions still in `pending_lead_review` doesn't notify early). `GET /api/bios/:brandId` now returns `review` (the latest decided steward_job's decision + required_changes + conditions + reject_reason_code + steward_notes + decided_at + steward_first_name) and `diff` (`diffBio(prevVersion, latest)` — field-level before/after, computed when the latest version was Steward-authored, i.e. `created_by` null + a recorded review). `diffBio` is pure over the BIO_FIELDS registry. Frontend (`portal-discovery.jsx`, additive to BioViewer): a **"From your Steward"** panel (return_changes → "needs a few changes" + list + edit CTA; reject → humanized reason + edit CTA; conditions → informational) and a collapsible **"What your Steward changed"** diff panel — both token-styled, gated on the new response fields; cert chip / SelfCertPanel / edit flow untouched. 121/121 unit tests; SPA build + boot clean. | Chose the structured request panel + diff (reuses the existing return-changes → edit → re-submit loop, defensible) over a free-form chat thread — held that as optional unless the user wants genuine back-and-forth (ponytail: a "here's what to fix" panel + re-submit closes ~90% of it). No new DB state machine, so verified at unit+build+boot level (the gate/cert/discovery e2e proofs cover the transitions). |
| 2026-08-15 | BIO v1.0 · M4 (read-contract) — the tiered agent read + provenance-to-agents + anti-invention guardrail | **The part that makes the BIO's honesty reach the 55 agents.** Fanned out 2 disjoint agents (specialist + Brandolph rewire) against a `getBioForAgent` contract I owned + built. **`bio-schema.js` gains `getBioForAgent({ bio, audience, slices, refusals, deepFields }) -> { blocks }`**: a brand-constant **T0 CORE** block (positioning/category/pillars/register/forbidden/primary-audience/northStar + refusals — one shared cache prefix across all specialists) + a per-department **T1 SLICE** block (specialists) or the full BIO (Brandolph), both `cache_control:ephemeral`. **Provenance-as-exceptions** rides in the text: low-confidence fields get an inline "(inferred — low confidence, do not assert as fact)" marker, and absent fields produce a "The BIO does not specify: … Do not invent these" gap line (via the new pure `computeAgentProvenance`, the machine sibling of `computeFocus`). T2 deep-fields param reserved (no deep field wired yet). **`compose-specialist-prompt.js`** (D): dropped the local `renderBioSlice`, routes the BIO layer through `getBioForAgent({audience:"specialist", slices: spec.bioSlices})`, and — the key quality fix — added the **anti-invention guardrail** to the L2 PLATFORM_PREAMBLE ("Never invent brand attributes the BIO does not contain … an invented tone, palette, or claim is the worst failure"), which the 55 specialists previously lacked (only L1 had it). **`prompt.js`** (E): Brandolph routes through `getBioForAgent({audience:"brandolph"})`, `renderBioLayer` + its `normalizeBio` import removed — BIO rendering now lives in ONE place for both L1 and L2. Both prompt builders keep the 4-layer cache-aware, OpenRouter/Anthropic-compatible block structure. 12 new/updated bio-schema tests + compose-specialist tests 6/6; **121/121** total; build + boot clean; runtime-verified core+slice+markers+refusals. | Scoped M4 to the read-contract + provenance + guardrail (highest-value, agent-facing). **Deferred (documented): the deep v1.0 field expansion** (identity.essence, positioning.statement/PODs, messaging house, voice exemplars, visual system) — it's additive to the registry + getBioForAgent already tiers/renders by registry, so deep fields drop in later without touching the composers. Two M0 tests that asserted the OLD renderBioLayer text were updated to the new tiered format (intentional format change, not a regression — the 6 new getBioForAgent tests prove the new output). Registry-migration-off-the-browser-mock still pending (pre-M4 item not yet done; getBioForAgent reads the registry from bio-schema.js directly, so it's unaffected). |
| 2026-08-15 | BIO v1.0 · M5 (foundation) — EN/ES/AR i18n + RTL + Arabic typography + locale data-model | Fanned out 3 disjoint agents. **i18n (`src/lib/i18n.js` + `src/locales/{en,es,ar}.json`, `main.jsx`, `portal-shell.jsx`):** a lean, dependency-free layer (~180 lines) over `Intl` — `t(key,vars)` with fallback chain + ICU-lite plurals via `Intl.PluralRules` (Arabic's 6 CLDR categories verified), `useLocale()` hook (useSyncExternalStore), `setLocale` writes `localStorage` + `document.documentElement.lang/dir` (rtl for ar), `fmtNumber/fmtDate`. Shell wired as the reference (nav/switcher/topbar/upgrade) with a compact EN/ES/العربية switcher; keys/comments English, only values localized; proper nouns kept. **RTL + Arabic type (`ds-tokens.css`, `portal.css`, `assets/fonts/`):** self-hosted **IBM Plex Sans Arabic** (4 weights, valid subsetted woff2, `unicode-range`-scoped cascade so one stack auto-routes Latin→Inter / Arabic→Plex Arabic), a `[lang=ar]/[dir=rtl]` token block (font swap, line-height 1.7, zeroed the negative display letter-spacing invalid for connected script); the hand-CSS migrated physical→logical (`padding-inline-*`, `inset-inline-*`, `text-align:start/end`, logical bubble radii) with an opt-in `[dir=rtl] .icon-directional{scaleX(-1)}`; **the InteractiveCanvas coordinate space left LTR by design** (chrome mirrors, spatial graph doesn't — per the earlier sign-off). **Locale data-model (`20260815030000_m5_locale.sql`, `locale.js`+test):** additive columns — `brands.default_locale/output_locales/numeral_system`, `bios.cert_locales`, `briefs.brief_lang` + `output_locales` (**language-of-brief ≠ language-of-output**), `runs.output_locale`, `outputs.locale`; a pure `locale.js` (resolveOutputLocales/normalizeLocale/isRtl, 6 tests). SPA build clean; 127/127 unit tests. | Scoped M5 to the FOUNDATION (infra + tokens + fonts + data-model) with the shell wired as the reference — NOT the exhaustive per-screen string translation (mechanical; now a fill-in-the-catalog follow using the established `useLocale()`+`t()` pattern). Also deferred (documented): per-locale `voice.byLocale` + per-locale QA (the native-authoring pipeline). M5-B (RTL/fonts) agent stalled on a redundant post-completion self-check (watchdog killed it) but its deliverables were complete + valid on disk (verified woff2 magic bytes, brace balance, no residual physical props) — I completed integration. The M5 locale migration is additive; left as a file awaiting apply (like the prior migrations). |

# CaastorOS — Implementation Log

Append-only. Format: `| DATE | TASK | WHAT WAS BUILT | DIVERGENCE + REASON |`

| DATE | TASK | WHAT WAS BUILT | DIVERGENCE + REASON |
|------|------|----------------|---------------------|
| 2026-05-22 | IA nav reorg (ia-plan.md) | `portal-shell.jsx`: added `section` field to `CLIENT_ROUTES`, grouped sidebar eyebrows, demoted `discovery` from nav (route guard patched), renamed `home` → "Create", `bio` active highlight on discovery route. | None — matches ia-plan.md §5 exactly. |
| 2026-05-22 | Wire HomeCreate brief→ship loop | `portal-brandolph.jsx` `HomeCreate`: added `phase` state (idle → sharpening → proposing → running → done), `handleStart` / `handleProceed` / `handleRun` / `handleReset` handlers, `⌘+↵` keyboard shortcut, review panel renders `BrandolphDiagnosis` then inline assembly grid with live `dot-state` indicators, "Assembling…" spinner (2.8 s timeout), `OutputsReady` on completion. Dashboard (In flight, Try something, Brandolph watching) hides while review is active and restores on reset. | Inline assembly card is a new layout (not `AssemblyPanel` sidebar) — `AssemblyPanel` is height:100% and sidebar-optimised; reusing it inline would break layout. Inline card shares the same data (`getAssembly`) and `dot-state` classes. |
| 2026-05-24 | QW-001 IA naming fix (rev-2 §2 LOCKED structure) | `portal-shell.jsx`: rewrote `CLIENT_ROUTES` to the four sections × two children structure (`Create / Briefs` · `BIO / Library` · `Specialists / Humans` · `Credits / Account`). Section eyebrows ARE the contents (slash-joined). Replaced `Brand Intelligence` nav label with `BIO`, `Human craft` with `Humans`, `Settings` with `Account`. Updated `TopBar.titles` eyebrows to match. Cleaned stale `Human craft` references in `portal-briefs.jsx:1535` (flash toast), `portal-data.js:27` (credit ledger sample), `portal-craft.jsx:109` (drawer eyebrow), `portal-craft.jsx:179` (upsell prose). | Route id `craft` preserved (not yet renamed to `humans`) per rev-2 §2.3 — full route-id rename deferred to the M4 cleanup PR alongside other workspace-shape touches in P7. The longform descriptor "Brand Intelligence Object" still appears in narrative copy (`portal-briefs.jsx` BIO node labels, `portal-discovery.jsx` eyebrow) — kept intentionally as the **concept** name; only the nav **label** is locked to "BIO". |
| 2026-05-24 | QW-002 `ShellMode` primitive (rev-2 §7 workspace state) | `portal-shell.jsx`: added `useShellMode(routeId)` hook + `WORKSPACE_ROUTES = {brief-detail, canvas, board}` set; hook sets `document.documentElement.dataset.shellMode` to `"default"` or `"workspace"`. Wired into `App()` after design settings. `portal.css`: added 25 lines of `[data-shell-mode="workspace"]` overrides — dock collapses 216→56px with `:hover` re-expansion, labels + section eyebrows + OS mark fade via opacity (100ms), topbar height shrinks 56→40px, title font 16→14px, `.app` padding-left 248→88px. All transitions in pure CSS (180ms ease for layout, 100ms for opacity) — JS only writes the attribute. | Used `data-shell-mode` attribute on `<html>` mirroring the existing `data-theme`/`data-palette`/`data-density` pattern from `useDesignSettings` so CSS authors have one consistent driver for global shell state. Hover-reveal model preferred over a JS-controlled collapsed/expanded state (no extra React state to sync, no flicker, browser handles the timing). The hidden labels stay in the DOM (opacity:0, not display:none) so screen readers can still announce nav items. |
| 2026-05-24 | QW-003 Three-bucket Discovery intake (rev-2 §5.3 mock-side) | `portal-discovery.jsx`: replaced the single "Brand guidelines · paste or upload" field in `DiscoveryStep1` with three labelled drop zones (`foundations` / `visual` / `voice`). New inline `BucketDropZone` component supports drag-drop AND click-to-upload via a hidden `<input type="file" multiple>`. Each zone shows: bucket label, helper copy, `readBy` department tag, file chips with `×` remove. Local state via `useDState({foundations:[], visual:[], voice:[]})`. The "Start extraction" button now logs the bucketed payload to console (preview of the P1-003 `POST /api/bios/:brandId/sources` contract) before calling `onNext`. Footer also shows total file count when files are queued. Removed the now-redundant "Upload guidelines instead" link from the helper row. | Kept the implementation contained inline to keep this PR small — `BucketDropZone` lives next to `DiscoveryStep1` rather than in `portal-shared.jsx`. If the same three-bucket pattern shows up at the BIO sources tab (`BioSources` in same file), promote to shared at that point. The `BUCKETS` const at the top of the new block is the contract that P1-003 backend will mirror — keep these labels stable. |
| 2026-05-24 | QW-004 OutputCard footer split + Steward chip (rev-2 §5.5 + §9, moat-defining) | `portal-data.js`: added `CI_BRAND.bioVersion = 7` and `CI_BRAND.steward = {firstName, fullName, role, certifiedAt}` mock. Steward firstName "Marina" matches the rev-2 plan's exemplar; surname differs from `CI_USER.name` (which is the brand founder "Marina Reyes") — known mock collision, harmless. `portal-shared.jsx`: rebuilt `OutputCard` footer with two render branches: **ClientFooter** (default, public) leads with `Composed by {specialist} · BIO v{n} · certified by {steward.firstName} · {date}` — NO model name; **DebugFooter** adds `routed via {model.label}` + `run {short_id}`. Team portal renders DebugFooter directly; client portal renders ClientFooter inline and slides DebugFooter in as a tooltip card on hover (`.output-card:hover .output-card__debug-tip`). 9 lines of CSS in `portal.css` for the hover transition (opacity + transform, 140ms ease). | Hover-reveal preferred over click-to-toggle because the spec says "no click required" (rev-2 plan QW-004 acceptance). `:focus-within` added alongside `:hover` so keyboard users (tab into the card) also see the debug attribution. The hidden tooltip uses `pointer-events:none` when invisible so it doesn't block clicks below. Model name `GPT-5` (from `CI_MODELS.gpt5.label`) flows through unmodified — when P3 wires `runs.model_used` for real, that field replaces the agent.model lookup. Output card now has `position:relative` so the absolutely-positioned tooltip anchors correctly. |
| 2026-05-24 | P0 backend bootstrap (P0-002 + P0-003 + P0-006) | **P0-002 (user-side):** Supabase staging project `yemzrohzqfuxmekcgnnh` provisioned. SUPABASE_URL + anon + service-role keys written to `server/.env` (gitignored); structure documented in `server/.env.example`. **P0-003 (schema):** `supabase/migrations/20260524183348_init.sql` (464 lines) — 18 tables (kernel + rev-2: `team_members`, `industries`, `templates`, `template_versions`, `steward_jobs`, plus `bios.certified_*`, `bio_sources.bucket`, `briefs.mode`, `uploads.bucket_hint`), 3 public_* views for the `?ask=` allowlist, RLS workspace-isolation policies on all client-facing tables + role-gated `steward_role` policy on `steward_jobs`, and `handle_new_auth_user` trigger that auto-creates `workspaces`+`brands`+`users` row on first magic-link sign-in. **P0-003 follow-up:** `20260524185323_grants_and_admin_rls.sql` (54 lines) — standard Supabase GRANTs to anon/authenticated/service_role + read-only RLS on the admin-curated tables (industries, specs, templates, template_versions). Both migrations pushed to remote via `supabase db push`. **8 V1 industries seeded** via REST bulk insert (CLI `db query` defaults to local; REST + service-role bypasses RLS and works against remote). **P0-006 (specs):** `scripts/seed-specs.mjs` reads `src/portal-data.js` via Node `vm.runInContext` sandbox (avoids duplicating the 33-row CI_AGENTS + 6 CI_DEPT_SPECS source of truth), constructs spec payloads with role/objective/method/voice/refusals/modelRouting/bioSlices, and upserts via REST. Run via `npm run seed:specs`. Verified: 33 spec rows in DB (26 active live, 7 inactive soon). Root `package.json` gained 3 scripts: `dev:server`, `dev:all` (via newly-added `concurrently` dev dep), `seed:specs`. | Pivoted from refinement's Cloudflare Workers plan because the existing `server/src/index.js` already shipped a working Hono-on-Node server with Anthropic SSE streaming — keep what works, defer Workers migration. Migration is split into two files (init + grants) because dashboard SQL editor auto-grants on table creation but CLI migrations don't — without the second migration, PostgREST returned 403 on every table even with the correct service-role key. The seed-specs script uses sandbox eval over ESM import-refactor of portal-data.js to keep the source of truth single during the P0→P3 transition window (mocks are still consumed by the SPA until P3 wires real DB reads). PAT `sbp_e88f...` was passed in chat to enable my `supabase link`/`db push` from this session — flagged for user revocation in the dashboard once we're done with this push. |
| 2026-05-25 | P0-004 auth wiring + DB bridge + P1 backend scaffolding | **P0-004 (auth wiring):** Added root `.env.local` with `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` + `VITE_API_BASE`; root `.gitignore` updated to exclude `.env*`. Installed `@supabase/supabase-js` at SPA root. New `src/lib/supabase-browser.js` exposes the anon client + `apiFetch(path, init)` helper that auto-attaches `Authorization: Bearer <jwt>` from the current session. **Rewrote `src/portal-auth.jsx`** — replaced the localStorage mock with real Supabase email+password auth (signUp + signInWithPassword + signOut); the `useSession()` hook + `window.CI_AUTH` surface preserved so portal-shell consumers don't change. Role resolved from the public.users row (populated by the `handle_new_auth_user` trigger we shipped). Login screen now toggles between sign-in and sign-up modes, surfaces Supabase error messages, and routes by resolved role. **DB bridge:** new `server/src/lib/supabase.js` (service-role admin client + per-request `userClient(jwt)`); new `server/src/middleware/auth.js` (Hono middleware — extracts JWT, validates via Supabase Auth, looks up workspace_id + role, attaches to `c.set('auth')`); new `server/src/lib/load-brand-bio.js` (loads brand + latest BIO from DB; falls back to the Vinilo seed when no BIO exists yet so the endpoint works pre-P1; supports `requireCertified` flag for when P1.5 Steward lands). **Refactored `server/src/routes/brandolph.js`** — `/api/brandolph/ask` now requires auth + loads brand+bio from DB instead of `data/vinilo.js`. Server CORS allowlist expanded to include `http://localhost:5174` (Vite landed there when 5173 was occupied). **P1 backend scaffolding:** new `server/src/routes/bios.js` mounted at `/api/bios`: `POST /:brandId/sources` writes one `bio_sources` row per submitted reference with the rev-2 §5.3 `bucket` field set; `GET /:brandId` returns the latest BIO + cert state. Ownership check on every call (workspace_id match enforced server-side). | Chose email+password over magic-link for dev: magic-link callback URL requires a Supabase dashboard config change (Authentication → URL Config → add `http://localhost:5174`) that adds setup friction; password works zero-config the moment "Confirm email" is toggled off in Supabase → Authentication → Settings. Sign-up flow surfaces a clear "check your inbox" message if confirmations are still required so the user knows what to do. The brandolph route now does a workspace-scoped DB lookup via the service-role client rather than relying on RLS through `userClient(jwt)`, because the service role + explicit workspace check in `loadBrandBio` is simpler than threading the JWT through and faster (no RLS check overhead on hot path). Pre-P1 BIO fallback (Vinilo seed) is logged with a warn so it's spottable in dev — once Discovery (P1) ships, real BIOs land and the fallback only fires for brand-new workspaces. File-upload via Supabase Storage NOT YET wired — `POST /sources` accepts URL/text refs only; the QW-003 UI's File chips will need a follow-up multipart upload route. |
| 2026-05-25 | OR-001..OR-007 hybrid model router (Anthropic direct + OpenRouter) | **Decision:** keep Anthropic SDK direct for Claude calls (preserves the §7 prompt-caching cost lever — 60–80% input-cost savings projected on assembly runs); route all non-Claude text (GPT-5, Gemini Flash/Pro) through OpenRouter; image/web/deck/search vendors get their own modules later. **Files added:** `server/src/lib/models/anthropic.js` (Anthropic SDK adapter, preserves `cache_control: ephemeral` on system blocks), `server/src/lib/models/openrouter.js` (raw fetch + SSE parser, OpenAI-compatible API, surfaces per-call `cost_usd` from OpenRouter's `usage.cost`), `server/src/lib/models/router.js` (single `streamCompletion({ spec, system, messages })` entry; dispatches by `spec.payload.modelRouting.primary` prefix — `anthropic/*`, `openrouter/*`, `vendor/*` reserved for non-text). Both adapters yield normalized event stream `{type:"token"\|"done"\|"error"}` so consumers (routes, runtime) don't need vendor-aware code. **Files changed:** `server/src/routes/brandolph.js` now delegates to router (was direct Anthropic); `scripts/seed-specs.mjs` got a MODEL_MAP that rewrites short keys (`opus`,`sonnet`,`haiku`,`gpt5`,`gemFlash`,`gemPro`,`flux`,`gptimage`,`recraft`,`exa`,`v0`,`gamma`,`framer`,`elevenlabs`) to vendor-prefixed routes (`anthropic/claude-opus-4-7`, `openrouter/openai/gpt-5`, `vendor/fal/flux-1.1-pro`, etc.). Re-seeded 33 specs — verified mix: 22 anthropic/, 4 openrouter/, 7 vendor/. **Files added (testing):** `scripts/test-router.mjs` exercises all three paths through the router; `npm run test:router`. Verified: Anthropic Haiku 1.2s/4 tokens; Gemini 2.5 Flash 430ms/$0.0000088; GPT-5 via OpenRouter dispatches correctly. **Cache observation:** Brandolph's 1215-token system prompt sits just above Sonnet's 1024-token cache minimum; cache_creation fires on calls 2+ but hits aren't consistent yet. P3 specialist runs will have 2000+ token system prompts (full spec + bio slice + brief context) where caching is robust. | Hybrid over pure-OpenRouter because OpenRouter's Anthropic-cache passthrough is flaky and the cost lever is too important to risk — direct Anthropic is the right tradeoff. The `vendor/*` prefix in modelRouting is a deliberate marker so the runtime knows "this is a non-text vendor, dispatch via image/web/etc module" without each caller checking. Router enforces `max_tokens >= 16` floor because GPT-5 rejects smaller values (the first router test discovered this — caller-supplied values are also floored to keep the contract uniform across vendors). OpenRouter exposes per-call `cost_usd` in usage, which we surface in the normalized usage block; Anthropic doesn't (we compute cost from token counts × pricing table at the cost-ledger layer, planned for P3). PAT/keys: Anthropic key `sk-ant-...` + OpenRouter key `sk-or-v1-...` were passed in chat and written to `server/.env`; flagged for user rotation. |
| 2026-06-06 | P1-FE live Discovery→BIO loop wiring (frontend consumes the real backend) | **`src/portal-discovery.jsx`:** new `useLiveBio({pollMs})` hook resolves the workspace's brand (via `window.getCurrentBrandId` → first-brand fallback), polls `GET /api/bios/:brandId`, and exposes `{brandId, brandName, bio, cert, refresh}` + a `team_members` lookup for the certifier's name. `DiscoveryStep1` now fires `POST /api/discovery/start` (controlled URL input, busy/error states) instead of just advancing the wizard; three-bucket `BucketDropZone` intake kept (files still local — Storage upload is the `/sources/upload` follow-up). `DiscoveryStep2Running` polls `/api/bios/:id` every 3s for a version bump (the compile-bio worker finishing) with a 90s safety fallback + crude scrape→vision→compile→done stage UI. `DiscoveryStep2Results` + `BioViewer` rewired off mocks: `payloadToFields`/`fieldsToPayload` map the API's nested BIO ↔ the viewer's flat tab arrays; Edit→Save calls `PATCH /api/bios/:id` (new version, Steward re-cert); Feed-Brandolph calls `POST /api/bios/:id/sources`; live Steward cert chip; "No canon yet" empty state; competitive tab stubbed (a32 extracts it later). **`src/portal-floater.jsx`:** live Brandolph chat streams from `POST /api/brandolph/ask` (SSE) with mock fallback; memory-driven contextual greeting from `GET /api/brandolph/memory?brandId=`; floater now also shows on the admin portal. **`src/portal-shared.jsx`:** current-brand store (`getCurrentBrandId`/`setCurrentBrandId`/`useCurrentBrandId`, localStorage + `brand:changed` event, exported to window); OutputCard client/debug attribution footer split (Steward chip, no model name on client); redrawn BIO/brief/Humans/Create/Library/Credits/Settings icons. **`src/portal-auth.jsx`:** full real-Supabase auth (signin/signup/forgot/recovery modes, deadlock-safe `onAuthStateChange`, synchronous bootstrap from stored session). **`src/main.jsx`:** import `portal-admin.jsx`. **`src/portal-craft.jsx`:** "Human craft"→"Humans" copy. **Bugfix:** `streamBrandolph` used raw `fetch` (no JWT) against the auth-required `/ask` route → silent 401 → always fell back to mock; switched to `apiFetch` so the JWT attaches and live chat works. | **Verified WITHOUT spending API credits** (standing "don't waste credits" rule): `npm run build` clean (85 modules, 820kB), `npm run test:units` green (47/47), server boots and every loop endpoint returns 401 unauth = wired+guarded (`POST /discovery/start`, `GET/PATCH /bios/:id`, `POST /bios/:id/sources`, `POST /brandolph/ask`, `GET /brandolph/memory`). **NOT yet run:** a real paid end-to-end Discovery (scrape + Gemini 2.5 Pro BIO synth) — left for the user to trigger so we don't burn credits on a CI-style check. The Discovery step records a *baseline* BIO version on first poll and advances only when a higher version lands, so a re-discovery on a brand that already has a BIO works correctly. Backend (`discovery.js`, `bios.js`, `compile-bio.js`, `assign-steward.js`, `/brandolph/memory`) was already committed earlier; this row is the frontend that consumes it. |
